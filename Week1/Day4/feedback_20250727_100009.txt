URL: https://github.com/FransheskaBER/DI-Bootcamp/blob/main/Week1/Day4/ExercisesXP.py
suggestions for improvement:
- In Exercise 1, while the code correctly uses set operations, it performs additional, unnecessary calculations with `difference`. The core functionality is correct, but the extra calculations are redundant.
- In Exercise 7, the initial approach to storing fruits in `user_fav_fruits` incorrectly treats the entire input as a single string. The corrected part is good, but the initial mistake lowers the score.
- Exercise 10 could be improved by using a `while` loop to directly iterate and remove 'Pastrami' instead of repeatedly checking with `while 'Pastrami' in sandwich_orders:` which can lead to more iterations than needed.
Brief justification:
- correctness: The code largely addresses the exercises correctly, demonstrating understanding of sets, tuples, lists, loops, and conditionals as outlined in the chapter content. However, some exercises contain extra, unnecessary calculations or minor logic flaws (as detailed in the feedback) which slightly reduce the score.
- readability: The code is generally well-structured and easy to follow. Variable names are mostly descriptive, and comments are provided where necessary. Minor improvements to variable naming or more concise logic could enhance readability further.
- performance: The code's performance is generally acceptable for the tasks involved.  However, in a few instances (Exercise 1 and Exercise 10), less efficient approaches could slightly impact performance with larger inputs, and this is addressed in the suggestions.
- security: The code does not present any security vulnerabilities, as it only interacts with user input for basic data entry without involving external resources or sensitive data.

