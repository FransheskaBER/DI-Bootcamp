URL: https://github.com/FransheskaBER/DI-Bootcamp/blob/main/Week_21/Day6/XP/exercises.js
suggestions for improvement:
- In Exercise 3, while type casting is demonstrated, it's not being fully utilized as the casted value isn't being used as a string (e.g., calling string-specific methods).
- In Exercise 4, the type assertion is implicitly performed by calling `toUpperCase()` on the first element, but the first element is not explicitly cast to a string using the `as` keyword.
- In Exercise 5, the generic constraint is met, but naming the function `longLength` is slightly misleading. A more descriptive name would improve readability.
- Consider adding more explicit type annotations for better code clarity, even where TypeScript can infer the types.
Correctness: 85%
-----
URL: https://github.com/FransheskaBER/DI-Bootcamp/blob/main/Week_21/Day6/XP/exercises.ts
suggestions for improvement:
- In Exercise 3, the `toUpperCase` method cannot be called without first ensuring `someValue` is indeed a string after the type assertion. Consider handling the case where it might not have the method or the type assertion fails.
- In Exercise 2, you can remove the `!value` check since number and string will always have a value, even if it is an empty string.
- In Exercise 4, you can implement a check to determine if the first element is of type string. If the first element is not a string, then a different approach may be required.
Correctness: 92%
-----