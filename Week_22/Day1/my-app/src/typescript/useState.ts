// const [count, setCount] = useState<number>(0);

// Breakdown:
// useState<number> → the state value must always be a number
// 0 → the initial state
// count → the current value
// setCount → a function to update the value

type User = {
    name: string;
    age: number;
};
// const [user, setUser] = useState<User>({ name: "Ana", age: 45 });

